// Write a function that takes two sets and returns their symmetric difference (elements in either set but not both).

const set1 = new Set([1, 2, 4, 6, 7, 8]);
const set2 = new Set([1, 4, 5, 9]);
console.log("symmetric difference one by two",set1.symmetricDifference(set2));